package extra;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;

import java.util.List;

//public class webDriverSyncDemoExplicit {

    //public static void main(String[] args) throws InterruptedException {

       // WebDriver driver = new ChromeDriver();
      //  driver.get("http://seleniumhq.org");
       // new WebDriverWait(driver,10)
             //   .until(ExpectedConditions.presenceOfElementLocated(By.id("gsc-i-id1")));
        //General timeouts
        // driver.manage().timeouts().pageLoadTimeout(15, TimeUnit.SECONDS);
        // driver.manage().timeouts().setScriptTimeout(15, TimeUnit.SECONDS);
        // driver.manage().timeouts().implicitlyWait(15, TimeUnit.SECONDS);

        //Waits

        //driver.get("http://seleniumhq.org");

       // WebElement searchInput = waitForElementLocatedBy(By.id("gsc-i-id1"));
        //searchInput.sendKeys("selenium java");
        //List<WebElement> searchBtn = driver.findElements(By.id("navbar"));
        //searchBtn.get(0).click();
        //new WebDriverWait(driver,10)
          //      .until(ExpectedConditions.presenceOfAllElementsLocatedBy(By.id("gs_cb50")));
       // List<WebElement> searchResults = driver.findElements(By.id("gs_cb50"));
        //Thread.sleep(2000);
      //  driver.quit();

//    }

  //  }
//}

